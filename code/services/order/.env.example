# Order servisi
ORDER_PORT=4004
NODE_ENV=development

# Ma'lumot bazasi (PostgreSQL — Docker compose)
DATABASE_URL="postgresql://beshariq:beshariq_dev_password@localhost:5432/order_db?schema=public"

# JWT (auth servisi bilan BIR XIL access secret bo'lishi shart)
JWT_ACCESS_SECRET=dev_access_secret_change_me

# Bog'liq servislar
RESTAURANT_SERVICE_URL=http://localhost:4003

# Yetkazib berish narxi (so'm) — MVP uchun qat'iy. Keyin Pricing servisi.
DELIVERY_FEE=5000
