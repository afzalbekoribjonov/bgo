name: beshariq_core
description: "Beshariq Super-App — ilovalar uchun umumiy data-layer (token, API, providerlar)."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.2
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.7.0
  flutter_riverpod: ^2.6.1
  flutter_secure_storage: ^9.2.2
  firebase_core: ^3.6.0
  firebase_messaging: ^15.1.3

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
